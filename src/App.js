// // import logo from './logo.svg';
// impact React from "react";
import './App.css';
import Header from "./header"

function App() {
  return (
   <div>
    <Header/>
    {/* <div>
                {data && Object.keys(data).map((key) =>console.log(data[key]))}
                <div>
                    <p>{data[key].name}</p>
                    <p>{data[key].email}</p>
                </div>
            </div> */}
   </div>
  );
}

export default App;
